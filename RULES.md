# PrTask

RULES.md must always be compact, complete, comprehensive, detailed, and required reading before any work. .NET 10 Blazor WASM + MAUI Android via SharedUI Razor Class Library (no FrameworkReference). SassCompiler build tool from NativeCdpBridge. No third-party runtime packages. PascalCase everywhere. SSO via GitHub OAuth, Google OIDC PKCE, Azure OIDC PKCE, Okta OIDC PKCE. Free platform — PrTask charges nothing to clients or developers. Clients post jobs with bounties for free, developers claim tasks and submit PRs, payment scales by PR complexity on approval and merge. All transactions in US Dollars (USD). Clients pay bounties via supported payment providers. Developers choose their preferred payout method. Leaderboard, earnings dashboard, XP, levels, streak, badges. Private source, public sanitized diffs. Night mode support — detect system prefers-color-scheme, default to OS current color scheme, theme switcher toggle in nav that persists choice to localStorage. Plain SCSS only (no CSS frameworks), generated CSS must be minified. SCSS layouts use flexbox. Google SSO must be functional for login — auto-login via Google when possible (prompt=none for silent auth, fall back to consent if needed). No horizontal scrolling past 100% width — #app div must be a flex column container with min-height: 100dvh and max-width: 100vw, word-break: break-word on body. Never use any overflow property (overflow-x, overflow-y, overflow: hidden, overflow-x: clip) — all overflow values forbid or break mobile touch scrolling in portrait mode. Never use 100vh — use 100dvh (dynamic viewport height accounts for mobile browser chrome). Vertical scrolling must work on all devices in both portrait and landscape orientations. All secrets must use dotnet 10 native secret handling (dotnet user-secrets) — never store secrets in source. Night mode support — detect system prefers-color-scheme, default to OS current color scheme. Plain SCSS only (no CSS frameworks), generated CSS must be minified. SCSS layouts use flexbox. Google SSO must be functional for login. Mobile containers must be viewport-contained — scrolling and dragging must not escape the container (overscroll-behavior: contain). All secrets must use dotnet user-secrets (Microsoft.Extensions.Configuration.UserSecrets) — never store secrets in source. Copy linters, analyzers, .editorconfig, E2E architecture tests from C:\work\NativeCdpBridge (do not touch that repo). Four analyzers: StyleCop, Roslynator, Meziantou, SonarAnalyzer — all PrivateAssets=all. Latest MSTest 4.x. Serverless architecture — one HTML file and one WASM file required to run the website, no backend server. OpenTofu + Cloudflare free tier. Utilize all free Cloudflare databases (D1 SQL, KV, R2, Durable Objects) so the serverless HTML+WASM app uses Cloudflare for all persistence. Domain has zero project references. Application references only Domain. Infrastructure references Application and Domain. E2E tests only — no unit tests required. Deploy latest HTML+WASM to Cloudflare using OpenTofu always. Comprehensive, detailed, complete, compact E2E tests must exist for every rule in RULES.md. E2E tests must verify https://prtask.com is live and working. Work in C:\work\prtask.com.

## Learnings

- Client project (PrTask.Client) has duplicate pages (ClientPricingPage, ClientHomePage, ClientHowItWorksPage) that mirror SharedUI pages — always update BOTH when changing content.
- Cloudflare Pages direct upload is incompatible with build_config in Terraform — use wrangler deploy without build_config block.
- Wrangler requires API Token (not Global API Key) — Memberships Read and User Details Read must be user-level scope, not account-level.
- MSTEST0032 flags Assert.AreEqual on compile-time constants — use source file content checks instead.
- Terraform state must be synced after out-of-band resource changes via `tofu state rm` + `tofu import`.
- DNS search suffix on local machine can cause resolution failures — use `curl --resolve` to bypass.
- `.wrangler/` directory contains account IDs — must be in .gitignore.
- Theme switcher uses inline `<script>` in index.html for flash-free initial load, `data-theme` attribute on `<html>`, and `@media (prefers-color-scheme: dark)` with `:not([data-theme="light"])` for CSS-only fallback.
- SonarAnalyzer S927 requires overridden method parameters to match base class names exactly (e.g., `firstRender` not `FirstRender` in `OnAfterRenderAsync`).
- Live site may serve stale content after deploy due to Cloudflare CDN caching old WASM DLLs — always purge cache after deploy: `POST /zones/{zone_id}/purge_cache` with `purge_everything: true` using Global API Key auth (wrangler token lacks cache purge permission).
- #app div must be flex column with min-height: 100dvh and max-width: 100vw. Never use any overflow property (clip, hidden, auto, scroll) — all overflow values forbid or break mobile portrait scrolling. Never use 100vh — use 100dvh instead. Google OAuth requires client_secret even with PKCE (Google design flaw) — store it in Cloudflare Worker secret binding.
- Payment model is NOT escrow — client is automatically charged when they approve a PR. No upfront deposit required.
- Pull requests are public. Multiple developers can submit PRs for the same task. Client chooses which PR to approve.
