namespace PrTask.Git.Generated;

internal static partial class Basics
{
    internal static void ReftableFree(nint P)
    {
    }

    internal static void ReftableBufInit(nint Buf)
    {
    }

    internal static void ReftableBufRelease(nint Buf)
    {
    }

    internal static void ReftableBufReset(nint Buf)
    {
    }

    internal static int ReftableBufSetlen(nint Buf, nuint Len)
        => default;

    internal static int ReftableBufCmp(nint A, nint B)
        => default;

    internal static int ReftableBufAdd(nint Buf, nint Data, nuint Len)
        => default;

    internal static int ReftableBufAddstr(nint Buf, string S)
        => default;

    internal static void FreeNames(string A)
    {
    }

    internal static nuint NamesLength(string Names)
        => default;

    internal static int ParseNames(string Buf, int Size, string Out)
        => default;

    internal static int NamesEqual(string A, string B)
        => default;

    internal static nuint CommonPrefixSize(nint A, nint B)
        => default;

    internal static uint HashSize(ReftableHash Id)
        => default;

}
